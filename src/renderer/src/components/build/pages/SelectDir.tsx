import React from 'react'

interface selectDirProps {
  triggerNext: any;
}

function SelectDir({triggerNext}: selectDirProps): React.JSX.Element {
  return (
    <div>
      THIS IS SELECT DIR
    </div>
  )
}

export default SelectDir
